@model IEnumerable<LeaveSetting>

@inject CustomIDataProtection protector;
@{
    ViewData["Title"] = "Shift Patterns List";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int i = 1;
}

<section role="main" class="content-body">
    <header class="page-header">
        <h2>Leave Settings</h2>

        <div class="right-wrapper text-right">
            <ol class="breadcrumbs">
                <li>
                    <a asp-controller="Home" asp-action="Index">
                        <i class="bx bx-bar-chart-alt-2"></i>
                    </a>
                </li>
                <li><span>Leave Settings</span></li>
            </ol>

            @*<a class="sidebar-right-toggle" data-open="sidebar-right"><i class="fas fa-chevron-left"></i></a>*@
        </div>
    </header>
    <section class="card">
        <header class="card-header">
            <h2 class="card-title">List of Leave Settings</h2>
        </header>
        <div class="card-body">
            <div class="row">
                <div class="col-sm-9">
                    <div class="m-0">
                        <a asp-controller="Setup" asp-action="AddLeaveSetting" class="btn btn-primary" style="background-color:cornflowerblue">Add Leave Setting<i class="fas fa-plus"></i></a>
                    </div>
                </div>
            </div>
            <table class="table table-responsive-md table-striped mb-0" id="datatable-tabletools">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>
                            Pay Grade
                        </th>                        
                        <th>
                            @Html.DisplayNameFor(model => model.LeaveType)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.LeaveDays)
                        </th>                        
                        <th>
                            @Html.DisplayNameFor(model => model.IsPaidLeave)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.IsCarryForward)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.MaxCarryForward)
                        </th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@(i++)</td>
                            <td>
                                @Html.DisplayFor(modelItem => item.PayGrade.Name)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.LeaveType)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.LeaveDays)
                            </td>                            
                            <td>
                                @Html.DisplayFor(modelItem => item.IsPaidLeave)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.IsCarryForward)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.MaxCarryForward)
                            </td>
                            <td>
                                <a asp-action="EditLeaveSetting" asp-route-id="@protector.Encode(item.LeaveSettingId.ToString())" style="color:magenta">Edit</a>
                            </td>                            
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <footer class="card-footer">
            <a class="btn btn-dark" asp-controller="Home" asp-action="Index"><i class="fas fa-chevron-left"></i> Back</a>
        </footer>
    </section>
</section>