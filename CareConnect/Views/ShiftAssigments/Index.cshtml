@model IEnumerable<CareConnect.Models.ShiftAssigment>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
@* <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Shift)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AssignedTo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AssignedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsDeclined)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateDeclined)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Shift.Duration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AssignedTo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AssignedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsDeclined)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateDeclined)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ShiftAssigmentId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ShiftAssigmentId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ShiftAssigmentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table> *@
